# 一篇文章彻底搞懂 Floyd 判圈算法（快慢指针）

> 刷题过程中，你是否也遇到过这样的困惑：为什么快慢指针能判断链表有环？为什么相遇后让一个指针回到起点，再次相遇就是环的入口？LeetCode 287「寻找重复数」明明是个数组题，怎么也能用快慢指针？
>
> 这篇文章会用**大量配图 + 逐步推演**的方式，把 Floyd 判圈算法的原理讲得明明白白。

---

## 目录

1. [从一个生活场景说起](#一从一个生活场景说起)
2. [阶段一：判断是否有环（LeetCode 141）](#二阶段一判断是否有环leetcode-141)
3. [阶段二：找到环的入口（LeetCode 142）](#三阶段二找到环的入口leetcode-142)
4. [进阶应用：寻找重复数（LeetCode 287）](#四进阶应用寻找重复数leetcode-287)
5. [总结](#五总结)

---

## 一、从一个生活场景说起

想象操场上有两个人在跑步：

- **小明**跑得慢，每分钟跑 1 圈
- **小红**跑得快，每分钟跑 2 圈

如果他们在**同一条直道上**跑，小红会越跑越远，两人永远不会相遇。

但如果他们在**环形跑道**上跑呢？小红速度快，一定会从后面追上小明——这就是**套圈**。

```
直道（无环）：  小明→  小红→→→    （小红越跑越远，永远追不上）
                 ———————————————————————

环形跑道（有环）：    ← ← ← ← ← ←
                   ↓           ↑
                   小明→  小红→→→    （小红绕一圈后追上小明！）
                   ↓           ↑
                   → → → → → →
```

Floyd 判圈算法的核心思想就是这个**套圈**原理——用一快一慢两个指针遍历链表，如果存在环，快指针一定会从后面追上慢指针。

---

## 二、阶段一：判断是否有环（LeetCode 141）

### 2.1 题目回顾

[141. 环形链表](https://leetcode.cn/problems/linked-list-cycle/)：给定一个链表，判断其中是否有环。

### 2.2 算法步骤图解

我们有一个带环的链表（节点值 3→2→0→-4，-4 指向 2 形成环）：

```
链表结构：
    3 → 2 → 0 → -4
        ↑         ↓
        ← ← ← ← ← ←
```

#### 初始状态：慢指针 `slow` 和快指针 `fast` 都指向头节点

```
     slow
     fast
      ↓
      3  →  2  →  0  →  -4
             ↑            ↓
             ← ← ← ← ← ← ←
```

#### 第 1 步：slow 走 1 步，fast 走 2 步

```
               slow
                ↓
      3  →  2  →  0  →  -4
             ↑     fast   ↓
             ← ← ← ← ← ← ←
                    ↓
```

- slow 走到节点 2
- fast 走到节点 0

#### 第 2 步：继续移动

```
                     slow
                      ↓
      3  →  2  →  0  →  -4
             ↑            ↓
             ← ← ← ← ← ← ←
                   fast(在节点2)
```

- slow 走到节点 0
- fast 从 0 走到 -4，再走到 2

#### 第 3 步：再次移动

```
                            slow(在节点-4)
                            ↓
      3  →  2  →  0  →  -4
             ↑            ↓
             ← ← ← ← ← ← ←
             fast(在节点0)
```

- slow 走到节点 -4
- fast 走到节点 0

#### 第 4 步：快慢指针相遇！

```
              slow
              fast 🎯 相遇！
               ↓
      3  →  2  →  0  →  -4
             ↑            ↓
             ← ← ← ← ← ← ←
```

- slow 走到节点 2
- fast 走到节点 2
- **slow == fast → 有环！**

### 2.3 为什么一定相遇？

很多文章只说"快指针速度是慢指针的 2 倍所以会追上"，但没有解释清楚**为什么不会刚好跳过**。

让我们用**相对速度**来理解：

- 慢指针每次走 1 步，快指针每次走 2 步
- 在慢指针看来，快指针每次向自己靠近 1 步（相对速度 = 1 步/轮）
- 只要环存在，快指针与慢指针之间的距离每次减少 1
- **距离不可能从 1 直接跳到 -1 跳过**，所以必然会相遇

```
以慢指针为参照物来看（慢指针静止，快指针每次靠近 1 格）：

距离 = 3:   slow · · · fast      （快指针在慢指针前方 3 格）
距离 = 2:   slow · · fast        （靠近 1 格）
距离 = 1:   slow · fast          （靠近 1 格）
距离 = 0:   slow/fast 🎯          （相遇！不会跳过）
```

### 2.4 代码实现

```java
public boolean hasCycle(ListNode head) {
    if (head == null || head.next == null) {
        return false;
    }

    ListNode slow = head;
    ListNode fast = head;

    while (fast != null && fast.next != null) {
        slow = slow.next;       // 慢指针走 1 步
        fast = fast.next.next;  // 快指针走 2 步

        if (slow == fast) {     // 相遇 = 有环
            return true;
        }
    }

    return false;  // 快指针到达 null = 无环
}
```

---

## 三、阶段二：找到环的入口（LeetCode 142）

### 3.1 题目回顾

[142. 环形链表 II](https://leetcode.cn/problems/linked-list-cycle-ii/)：不仅要判断是否有环，还要找到环的**入口节点**。

### 3.2 关键数学推导（配图版）

这是整个算法最难理解的部分，我会一步步拆解。

#### 先定义几个距离

```
        环入口
          ↓
  1 → 2 → 3 → 4 → 5
          ↑       ↓
          ↑       ↓
          8 ← 7 ← 6
```

设：
- **a** = 从链表头到**环入口**的距离（节点 1→2→3，a = 2）
- **b** = 从环入口到**相遇点**的距离（沿环走）（节点 3→4→5→6，b = 3）
- **c** = 从相遇点**绕回**环入口的距离（节点 6→7→8→3，c = 3）
- 环的长度 **L = b + c** = 6

```
距离标注图（展开视角）：

  |←—— a = 2 ——→|←—— b = 3 ——→|←—— c = 3 ——→|
  
  头节点        环入口        相遇点        (绕回环入口)
   ↓            ↓            ↓
   1  →  2  →  3  →  4  →  5  →  6  →  7  →  8
               ↑                              ↓
               ← ← ← ← ← ← ← ← ← ← ← ← ← ← ←
```

#### 推导相遇时两指针走过的路程

当快慢指针在相遇点碰面时：

- **慢指针走过的距离**：`a + b`
  - 解释：从头部走到环入口（a），再走到相遇点（b）
  - 为什么慢指针不会在环里绕很多圈？因为快指针速度是它的 2 倍，慢指针走完第一圈之前就会被追上

- **快指针走过的距离**：`a + b + n·L`
  - 解释：从头部走到环入口（a），走到相遇点（b），但在此之前已经在环里多绕了 n 圈（n ≥ 1）

```
慢指针的路径：
  头 ──a──→ 入口 ──b──→ 相遇点  （一条直线，不绕圈）

快指针的路径：
  头 ──a──→ 入口 ──绕n圈──→ 入口 ──b──→ 相遇点  （多绕了 n 圈）
```

#### 核心等式

因为快指针速度是慢指针的 **2 倍**，相同时间内距离也是 2 倍：

```
2 × (慢指针距离) = 快指针距离

2 × (a + b) = a + b + n·L

简化：
2a + 2b = a + b + n·L
a + b = n·L
a = n·L - b
```

由于 `L = b + c`，带入：

```
a = n·(b + c) - b
a = n·b + n·c - b
a = (n-1)·b + n·c
a = (n-1)·b + (n-1)·c + c
a = (n-1)(b + c) + c
```

#### 关键结论

**`a = (n-1)·L + c`**

用人话说就是：

> **从头节点到环入口的距离 `a`，等于从相遇点走 `c` 步（到达入口），再绕环 `(n-1)` 圈后回到入口的距离。**

这意味着：如果我们把一个指针放在**头节点**，另一个放在**相遇点**，两者同时以**相同速度**（每次 1 步）移动，它们会在**环入口**相遇！

```
图解 (n=1 的情况，即 a = c)：

指针1：头节点 ——a 步——→ 环入口
指针2：相遇点 ——c 步——→ 环入口

因为 a = c，且两者速度相同，所以同时在环入口相遇 🎯

  指针1 起点(头)                         指针2 起点(相遇点)
      ↓                                      ↓
      1  →  2  →  3  →  4  →  5  →  6  →  7  →  8
                  ↑                              ↓
                  ← ← ← ← ← ← ← ← ← ← ← ← ← ← ←
                  
  走 2 步后：

                  指针1
                   ↓
      1  →  2  →  3  →  4  →  5  →  6  →  7  →  8
                  ↑   指针2                      ↓
                  ← ← ← ← ← ← ← ← ← ← ← ← ← ← ←
                  ↑
                  指针2 走了 2 步到这里
```

### 3.3 完整流程演示

以 LeetCode 官方示例 `[3, 2, 0, -4]`，pos=1（-4 指向 2）为例：

```
链表结构（标注 a, b, c）：

     a=1        b=2         c=1
    ───→     ───────→     ───→
   3  →  2  →  0  →  -4
         ↑            ↓
         ← ← ← ← ← ← ←
         
  环入口=节点2, 环长L=b+c=3
```

#### 阶段一：快慢指针找相遇点

```
初始：
      slow,fast
         ↓
    3 → 2 → 0 → -4
        ↑        ↓
        ← ← ← ← ←

第1轮：
    slow = slow.next = 2
    fast = fast.next.next = 0

              slow
               ↓
    3 → 2 → 0 → -4
        ↑        ↓
        ← ← ← ← ←
               ↑
              fast

第2轮：
    slow = slow.next = 0
    fast = fast.next.next = 2   (0→-4→2)

                     slow
                      ↓
    3 → 2 → 0 → -4
        ↑        ↓
        ← ← ← ← ←
        ↑
       fast

第3轮：
    slow = slow.next = -4
    fast = fast.next.next = -4  (2→0→-4)

                      slow
                      fast  ← 相遇！🎯
                       ↓
    3 → 2 → 0 → -4
        ↑        ↓
        ← ← ← ← ←
```

#### 阶段二：找环入口

```
初始：
    ptr 从头节点(3)出发，slow 从相遇点(-4)出发，每次都只走 1 步

    ptr                     slow
     ↓                       ↓
    3 → 2 → 0 → -4
        ↑        ↓
        ← ← ← ← ←

第1轮（ptr 和 slow 各走 1 步）：
    ptr = 3.next = 2
    slow = -4.next = 2

          ptr
          slow  ← 再次相遇！🎯
           ↓
    3 → 2 → 0 → -4
        ↑        ↓
        ← ← ← ← ←

结果：节点 2 就是环的入口！
验证：a=1, c=1，a=c，ptr 和 slow 同时到达入口 ✓
```

### 3.4 代码实现

```java
public ListNode detectCycle(ListNode head) {
    if (head == null || head.next == null) {
        return null;
    }

    ListNode slow = head;
    ListNode fast = head;

    // 阶段一：找相遇点
    while (fast != null && fast.next != null) {
        slow = slow.next;
        fast = fast.next.next;
        if (slow == fast) {
            // 阶段二：找环入口
            ListNode ptr = head;
            while (ptr != slow) {
                ptr = ptr.next;
                slow = slow.next;
            }
            return ptr;  // 再次相遇 = 环入口
        }
    }
    return null;  // 无环
}
```

### 3.5 一张图总结两阶段

```
┌─────────────────────────────────────────────────────────┐
│                  Floyd 判圈算法 两阶段                    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│   阶段一：判断有环                                        │
│   ┌──────────┐     ┌──────────┐                         │
│   │  slow=快  │     │  slow=步  │    相遇 → 有环          │
│   │  fast=慢  │ →→→ │  fast=步  │ →→→ 到头 → 无环          │
│   └──────────┘     └──────────┘                         │
│                                                         │
│   阶段二：找环入口                                        │
│   ┌──────────┐     ┌──────────┐     ┌──────────┐       │
│   │ slow=相遇点│     │ ptr,slow  │     │ 再次相遇  │       │
│   │ ptr = 头  │ →→→ │ 各走1步  │ →→→ │ = 环入口  │       │
│   └──────────┘     └──────────┘     └──────────┘       │
│                                                         │
│   数学原理：a = (n-1)L + c                               │
│   从头到入口的距离 = 从相遇点绕到入口的距离                  │
└─────────────────────────────────────────────────────────┘
```

---

## 四、进阶应用：寻找重复数（LeetCode 287）

### 4.1 题目回顾

[287. 寻找重复数](https://leetcode.cn/problems/find-the-duplicate-number/)：

> 给定一个包含 `n+1` 个整数的数组 `nums`，数字都在 `[1, n]` 范围内。只有一个重复数字，找出它。
>
> **限制**：不能修改数组，只能用 O(1) 额外空间。

### 4.2 核心思维转换：数组 → 链表

这是最精彩的部分——**把数组看成一个隐式的链表**。

数组的每个位置 `i` 是一个"节点"，`nums[i]` 就是它的 `next` 指针指向的下标。

```
举例：nums = [1, 3, 4, 2, 2]

索引:   0   1   2   3   4
值:     1   3   4   2   2

画成链表：
  0 → 1 → 3 → 2 → 4
              ↑    ↓
              ← ← ←
              
因为 nums[4] = 2，所以 4 指向 2，形成环！
环的入口是 2，而 2 正是重复的数字。
```

**为什么会形成环？**

因为数字范围是 `[1, n]`，数组长度是 `n+1`，索引范围是 `[0, n]`。所有值都 ≥ 1，所以不存在值为 0 的元素，这意味着**从索引 0 出发，永远走不到 null**。加上只有一个重复数，必然形成一个环，而**环的入口就是重复的数**。

```
推导：

1. 值范围 [1, n]，索引范围 [0, n]
2. 索引 0 的值一定 ≥ 1，所以从 0 出发一定在有效范围内
3. 只有一个数重复 → 只有一个节点被两个指针指向 → 这是环入口的特征
4. 所以：环入口的索引 = 重复的那个数
```

### 4.3 图解算法流程

以 `nums = [1, 3, 4, 2, 2]` 为例：

```
数组映射：
  nums[0] = 1  →  0 指向 1
  nums[1] = 3  →  1 指向 3
  nums[2] = 4  →  2 指向 4
  nums[3] = 2  →  3 指向 2
  nums[4] = 2  →  4 指向 2

链表视角：
      0 → 1 → 3 → 2 → 4
                  ↑    ↓
                  ← ← ←
```

#### 阶段一：找相遇点

```
初始：slow = 0, fast = 0

步骤1: slow = nums[0] = 1
       fast = nums[nums[0]] = nums[1] = 3

步骤2: slow = nums[1] = 3
       fast = nums[nums[3]] = nums[2] = 4

步骤3: slow = nums[3] = 2
       fast = nums[nums[4]] = nums[2] = 4

步骤4: slow = nums[2] = 4
       fast = nums[nums[4]] = nums[2] = 4  →  相遇！ 🎯
```

#### 阶段二：找入口（重复数）

```
相遇后：slow 在索引 4，另起 ptr 从索引 0 出发，都每次走 1 步

步骤1: ptr = nums[0] = 1
       slow = nums[4] = 2

步骤2: ptr = nums[1] = 3
       slow = nums[2] = 4

步骤3: ptr = nums[3] = 2
       slow = nums[4] = 2  →  相遇！ 🎯

返回值 2 就是重复的数！
```

### 4.4 代码实现

```java
public int findDuplicate(int[] nums) {
    // 阶段一：快慢指针找相遇点
    int slow = 0;
    int fast = 0;

    do {
        slow = nums[slow];           // 走 1 步
        fast = nums[nums[fast]];     // 走 2 步
    } while (slow != fast);

    // 阶段二：找环入口 = 重复数
    int ptr = 0;
    while (ptr != slow) {
        ptr = nums[ptr];    // 从头部出发，走 1 步
        slow = nums[slow];  // 从相遇点出发，走 1 步
    }

    return ptr;  // 相遇点 = 环入口 = 重复数
}
```

### 4.5 为什么这么做是正确的？

回顾链表版本的公式 `a = (n-1)L + c`，这里完全适用：

```
数组版含义：
  a = 从索引 0 到环入口（重复数第一次出现的索引）的步数
  c = 从相遇点到环入口的步数
  L = 环的长度

因为 a = (n-1)L + c，ptr 从 0 走 a 步到入口，
slow 从相遇点走 c 步（加上 (n-1)L 圈）也到入口，
所以它们一定在环入口相遇。
```

---

## 五、总结

### 算法模板

Floyd 判圈算法可以总结为以下通用模板：

```
阶段一（找相遇点）：
  slow = 起点, fast = 起点
  while slow != fast:
      slow = next(slow)         // 走 1 步
      fast = next(next(fast))   // 走 2 步

阶段二（找环入口）：
  ptr = 起点
  while ptr != slow:
      ptr = next(ptr)    // 走 1 步
      slow = next(slow)  // 走 1 步
  return ptr  // 环入口
```

### 适用场景

| 题目 | 题型 | 关键技巧 |
|------|------|----------|
| LeetCode 141 | 判断链表有环 | 快慢指针是否相遇 |
| LeetCode 142 | 找链表环入口 | 两阶段：相遇 + 同步走 |
| LeetCode 287 | 数组找重复数 | 数组 → 链表建模 |
| LeetCode 202 | 快乐数 | 判断数字变换是否有环 |

### 核心要点

1. **相对速度为 1** 保证不会"跳过"，必然相遇
2. **`a = (n-1)L + c`** 是从相遇点找入口的数学基础
3. **数组题也能建模成链表**，关键在于理解 `nums[i]` 就是 `next` 指针
4. 时间复杂度 O(n)，空间复杂度 O(1)，是真正的"原地"算法

---

> 如果这篇文章对你有帮助，欢迎点赞收藏。有疑问可以在评论区留言交流 😊

---

*本文涉及的完整代码见：*
- `linkedList/hasCycle.java` — LeetCode 141 完整 ACM 模式代码
- `linkedList/whereHasCycle.java` — LeetCode 142 完整 ACM 模式代码
- `findDuplicateSolution.java` — LeetCode 287 完整 ACM 模式代码
